import { Router } from "express";

export const userRouter: Router = Router();

// User routes have been removed.
// This app uses anonymous session-based identity.
// See /api/v1/auth/me for session info.
